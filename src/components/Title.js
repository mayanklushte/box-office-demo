import React from 'react'

const Title = ({title, subtitle}) => {
    return (
        <div>
            <h1>{title}</h1>
            <h4>{subtitle}</h4>
            
        </div>
    )
}

export default Title
